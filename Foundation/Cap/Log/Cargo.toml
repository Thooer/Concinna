[package]
name = "cap_log"
version = "0.1.0"
edition = "2021"

[lib]
name = "cap_log"
path = "API/lib.rs"

[dependencies]
cap_stream = { path = "../Stream" }
prm_time = { path = "../../Prm/Time" }
cap_path = { path = "../Path" }
