# Input 模块

## 1. 模块定位

* **职责**：提供跨平台的输入系统原语接口，包括输入初始化和游戏手柄状态获取。
* **边界**：不处理复杂的输入映射、事件处理或高级输入逻辑。
* **外部依赖**：无

## 2. 设计

采用平台抽象层设计，提供统一的输入系统接口，支持多种平台实现（Windows、Noop）。

## 3. API

* `Input::Initialize`：初始化输入系统。
* `Input::Shutdown`：关闭输入系统。
* `Gamepad::GetState`：获取指定索引的游戏手柄状态。

## 4. Samples

无