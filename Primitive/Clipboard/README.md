# Clipboard 模块

## 1. 模块定位

* **职责**：提供跨平台的剪贴板文本操作接口。
* **边界**：仅处理文本类型的剪贴板操作，不支持其他类型（如图片、文件）。
* **外部依赖**：无

## 2. 设计

采用平台抽象层设计，提供统一的剪贴板文本读写接口，支持多种平台实现（Windows、Noop）。

## 3. API

* `Clipboard::SetText`：设置剪贴板文本内容。
* `Clipboard::GetText`：获取剪贴板文本内容。

## 4. Samples

无