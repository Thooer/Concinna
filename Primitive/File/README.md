# File 模块

## 1. 模块定位

* **职责**：提供跨平台的文件路径操作接口。
* **边界**：仅处理文件路径相关操作，不涉及文件内容读写。
* **外部依赖**：无

## 2. 设计

采用平台抽象层设计，提供统一的文件路径操作接口，支持多种平台实现（Windows、Noop）。

## 3. API

* `FilePath::Normalize`：规范化文件路径。
* `FilePath::Join`：连接两个文件路径。
* `FilePath::Basename`：获取文件路径的基名。

## 4. Samples

无